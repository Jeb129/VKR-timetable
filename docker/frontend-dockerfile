FROM node:20-alpine AS builder

WORKDIR /app

COPY frontend/package*.json ./
RUN npm install
RUN npm audit fix

COPY frontend/ ./
RUN npm run build

CMD ["sh", "-c", "rm -rf /output/* && cp -rp /app/dist/. /output/"]